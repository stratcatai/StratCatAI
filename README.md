# Strat Cat

One-page site for the Strat Cat YouTube channel — AI for restaurant operators.

Static site (single `index.html`). Edit the `CONFIG` block at the bottom of `index.html` to set your channel URL, Formspree email endpoint, and featured video IDs.
